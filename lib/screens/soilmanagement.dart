import 'package:flutter/material.dart';

class SoilManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Soil Management")),
      body: Center(child: Text("Soil Management Feature Coming Soon!")),
    );
  }
}

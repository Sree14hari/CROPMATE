import 'package:flutter/material.dart';

class CropSelectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Crop Selection")),
      body: Center(child: Text("Crop Selection Feature Coming Soon!")),
    );
  }
}
